# cc-bash-guard

Policy-only proxy for CLI invocations used by AI agents and shell hooks.

This README documents current user-facing behavior. Planned or proposed
behavior belongs in [`docs/dev/spec`](docs/dev/spec/README.md) and must not be
treated as shipped unless the spec status is `implemented` and the behavior is
covered by tests.

`cc-bash-guard` never rewrites commands. It parses the input command string,
evaluates permission policy, and returns `allow`, `ask`, or `deny`. The command
string passed through the hook remains the original command.

If you use RTK rewriting, use `cc-bash-guard hook --rtk` as the single Bash
hook. In that mode `cc-bash-guard` still evaluates permissions first, then
delegates the rewrite step to the external `rtk rewrite` command. Do not
register RTK as a second Bash hook.

Parser-backed normalization is evaluation-only:

- `bash -c 'git status'` and supported shell-wrapper forms are evaluated by
  parsing the inner command.
- `/usr/bin/git status` matches `command.name: git`, while the original program
  token remains available in trace/debug output.
- `aws --profile myprof sts get-caller-identity`,
  `aws --profile=myprof sts get-caller-identity`, and
  `AWS_PROFILE=myprof aws sts get-caller-identity` expose the same AWS semantic
  `profile`, `service`, and `operation` fields for permission matching.

## Current Status

Stable user-facing features:

- Claude Code hook integration via `cc-bash-guard hook`
- optional `cc-bash-guard hook --rtk` RTK integration that runs permission
  evaluation before external `rtk rewrite`
- permission buckets `deny`, `ask`, and `allow`
- permission rules using `command`, `env`, and `patterns`
- top-level `include` for splitting policy and E2E tests across local YAML files
- command-specific semantic parsers for higher-risk tools such as `git`, `gh`,
  `aws`, `kubectl`, `helmfile`, and `argocd`
- verified hook artifacts generated by `cc-bash-guard verify`
- `doctor`, `verify`, `init`, `version`, `hook`, and `semantic-schema`

Top-level `rewrite`, rewrite primitives, rewrite-local tests, permission
`match`, and singular `pattern` are not supported. If a config contains
top-level `rewrite`, `verify` fails with migration guidance.

## Typical Workflow

```sh
cc-bash-guard init
$EDITOR ~/.config/cc-bash-guard/cc-bash-guard.yml
cc-bash-guard verify
```

Register Claude Code with a `PreToolUse` Bash hook:

```json
{
  "matcher": "Bash",
  "hooks": [
    { "type": "command", "command": "cc-bash-guard hook" }
  ]
}
```

By default, `cc-bash-guard hook` fails closed when the verified artifact is
missing or stale. Run `cc-bash-guard verify` after editing policy. The
`--auto-verify` flag regenerates artifacts during hook execution and should be
used only when that review tradeoff is acceptable.

Hook decisions are returned as Claude Code `PreToolUse` JSON on stdout:

```json
{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "allow",
    "permissionDecisionReason": "cc-bash-guard permission evaluated"
  }
}
```

`permissionDecision` is `allow`, `ask`, or `deny`. `allow` skips Claude Code's
permission prompt, `ask` asks the user to confirm, and `deny` blocks the Bash
tool call. `permissionDecisionReason` uses the matched rule `message` when one
is configured; for `deny` Claude Code shows that reason to Claude, while
`allow` and `ask` reasons are user-facing.

`cc-bash-guard hook` exits `0` after printing valid hook JSON, including JSON
that contains `permissionDecision: "deny"`. Claude Code only processes hook
JSON from stdout on process success; using a non-zero exit would make Claude
Code ignore the structured `permissionDecision` payload and treat the result as
an exit-code-based hook error instead. Invalid input and missing or stale
verified artifacts therefore fail closed by returning deny JSON with exit `0`.

If you also use RTK command rewriting, do not register RTK as a second Bash
hook. Use one hook instead:

```json
{
  "matcher": "Bash",
  "hooks": [
    { "type": "command", "command": "cc-bash-guard hook --rtk" }
  ]
}
```

With `--rtk`, `cc-bash-guard` evaluates permissions first. If the command is not
denied, it invokes external `rtk rewrite` once and emits `updatedInput.command`
only when RTK returns a different command.

## Config Includes

Use top-level `include` to split policy and E2E tests into local YAML files:

```yaml
include:
  - ./policies/git.yml
  - ./policies/aws.yml
  - ./tests/git.yml

permission:
  ask:
    - name: project fallback
      patterns:
        - "^helm\\s+upgrade\\b"

test:
  - in: "helm upgrade app chart"
    decision: ask
```

Include paths are local file paths. Relative paths are resolved from the file
that declares the include, including nested includes. URLs, shell expansion,
environment variable expansion, command substitution, and globbing are not
supported. Empty include entries, missing files, non-regular files, and include
cycles fail verification. Symlinks follow the same behavior as normal config
file reads: paths that resolve to regular files are read as those files.

Included files are resolved first, then the current file. `permission.deny`,
`permission.ask`, `permission.allow`, and top-level `test` entries are
concatenated in that effective order. `cc-bash-guard verify` bundles the
resolved effective config into one verified artifact for hook execution. The
hook evaluates that artifact; included files are not loaded as separate policy
inputs at decision time. Changing any included file makes the artifact stale, so
run `cc-bash-guard verify` after editing included policy or tests.

## Permission Model

Permission evaluation is deterministic:

1. parse the original command string into a `CommandPlan`
2. evaluate `deny`
3. evaluate `ask`
4. evaluate `allow`
5. return `abstain` when no permission rule matches
6. merge permission sources with `deny > ask > allow > abstain`

Claude settings.json permissions and cc-bash-guard policy are both permission
sources. `abstain` means no matching rule. `deny` always wins, and an explicit
`ask` is not overridden by `allow` from another source. The final fallback is
`ask` only when all sources abstain. No configuration is required to choose
merge behavior. For compound commands such as `A && B`, permission sources are
merged for each command element before the compound decision is aggregated.

Each rule uses `command`, `env`, or `patterns`. `command` and `patterns` cannot
be combined in one rule. `env` can be combined with either. Shell `-c`
wrappers such as `bash -c 'aws s3 ls'` are unwrapped for evaluation:
`command` rules and `patterns` rules both evaluate the inner command while the
executed command string is preserved.

```yaml
permission:
  deny:
    - name: git destructive force push
      command:
        name: git
        semantic:
          verb: push
          force: true
      message: "force push is blocked"

  allow:
    - name: aws identity
      command:
        name: aws
        semantic:
          service: sts
          operation: get-caller-identity
      env:
        requires:
          - AWS_PROFILE
      message: "allow AWS identity check"

  ask:
    - name: helm upgrade fallback
      patterns:
        - "^helm\\s+upgrade\\b"
      env:
        requires:
          - KUBECONFIG
      message: "helm upgrade requires confirmation"
```

`message` is surfaced as Claude Code's permission decision reason when a rule
determines `allow`, `ask`, or `deny`.

`command.name` selects the semantic parser. Fields under `command.semantic`
are interpreted in that parser's namespace, so no extra tool-name nesting under
`semantic` is required.

Use `patterns` for commands without a semantic parser, including read-only
basics such as `ls`, `cat`, `grep`, `head`, `tail`, and `pwd`. Patterns match
the original command string and each parsed command element, including shell
`-c` inner commands:

```yaml
permission:
  allow:
    - name: read-only shell basics
      patterns:
        - "^ls(\\s+-[A-Za-z0-9]+)?\\s+[^;&|`$()]+$"
        - "^pwd$"
        - "^cat\\s+[^;&|`$()]+$"
        - "^head\\s+[^;&|`$()]+$"
        - "^tail\\s+[^;&|`$()]+$"
```

### Safe Pattern Rules

`patterns` are a fallback for commands that do not have command-specific
semantic parsers. Prefer `command` plus `command.semantic` for supported tools
such as `git`, `gh`, `aws`, `kubectl`, `helmfile`, and `argocd`; those rules
can distinguish read-only operations from destructive ones.

When you do need `patterns`, keep them narrow:

- anchor regexes with `^` and `$`, or with explicit token boundaries such as
  `(\\s|$)`
- allow only the subcommands you intend, as in the Terraform example below
- avoid broad allow patterns such as `.*`, `^aws\\s+`, `^terraform\\s+`, or
  `^npm\\s+`
- account for shell metacharacters such as `;`, `&`, `|`, backticks, `$()`,
  redirects, subshells, and process substitution
- remember that an allowed command can invoke scripts, plugins, or subcommands
  that cc-bash-guard does not deeply inspect, such as `npm run lint` or
  `make test`

Use top-level `test` entries with `cc-bash-guard verify` to pin both safe and
unsafe examples:

```yaml
permission:
  allow:
    - name: terraform read-only fallback
      patterns:
        - "^terraform\\s+(plan|show)(\\s|$)[^;&|`$()]*$"

test:
  - in: "terraform plan -out=tfplan"
    decision: allow
  - in: "terraform apply -auto-approve"
    decision: ask
  - in: "terraform plan; terraform apply -auto-approve"
    decision: ask
```

## AWS Style

Preferred AWS command style in user and project guidance:

```sh
AWS_PROFILE=myprof aws eks list-clusters
```

Non-preferred style:

```sh
aws --profile myprof eks list-clusters
```

`cc-bash-guard` does not convert between these forms. It parses AWS
`profile`, `service`, and `operation` semantically for policy evaluation. Use
policy `ask` or `deny` rules for ambiguous or dangerous command styles.

`cc-bash-guard` does not convert between these AWS forms.

## Shell Evaluation

Supported shell `-c` wrappers are evaluated by parsing the inner command for
permission decisions:

- `bash -c 'git status'`
- `sh -c 'git status'`
- `/bin/bash -c 'git status'`
- `env bash -c 'git status'`
- `command bash -c 'git status'`
- `sudo -u root bash -c 'git status'`
- `timeout 10 bash -c 'git status'`
- `busybox sh -c 'git status'`

This is not command rewriting. The command returned to the hook remains the
original command, except when `hook --rtk` delegates to external RTK rewriting.
Non-`-c` shell invocations such as `bash script.sh` are not expanded. Unsafe or
unparsable inner shell commands fail closed.

## CLI Help

```sh
cc-bash-guard help config
cc-bash-guard help match
cc-bash-guard help semantic
cc-bash-guard semantic-schema --format json
```
